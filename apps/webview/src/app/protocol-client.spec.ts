import { describe, it } from 'vitest'

describe('ProtocolClient', () => {
  it.todo('validates messages before resolving requests or publishing events')
  it.todo('matches exactly one response to each request id')
  it.todo('rejects every pending request on page disposal')
  it.todo('does not persist prompts, tool output, endpoints, or credentials')
})
