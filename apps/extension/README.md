# DeepSeek Harness for VS Code

This directory is the packaged VS Code extension. The repository is currently an implementation scaffold: production behavior is intentionally marked with `TODO` requirements and is not yet usable.

Development, architecture, and capability instructions live in the repository root documentation.
