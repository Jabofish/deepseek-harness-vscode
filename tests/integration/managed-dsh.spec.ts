import { describe, it } from 'vitest'

describe('managed DSH integration', () => {
  it.todo('starts dsh web on loopback port zero and captures the resolved port')
  it.todo('shares one managed process among simultaneous view and command activation')
  it.todo('shuts down only the managed process on extension deactivation')
})
