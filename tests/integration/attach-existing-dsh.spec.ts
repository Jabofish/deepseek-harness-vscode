import { describe, it } from 'vitest'

describe('existing DSH integration', () => {
  it.todo('attaches to a fixture DSH server and does not invoke the spawn dependency')
  it.todo('recovers the event stream after one transient disconnect without duplicate events')
  it.todo('leaves the external fixture process running when the client disposes')
})
