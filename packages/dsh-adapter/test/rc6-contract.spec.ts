import { describe, it } from 'vitest'

describe('DeepSeek Harness 0.1.0-rc.6 contract', () => {
  it.todo('matches every RPC name used by repositories against the pinned official rpc-map')
  it.todo('maps every official host and mux event family into a domain event')
  it.todo('validates success, error, and malformed response fixtures')
  it.todo('covers session, workspace, model, provider, setting, credential, goal, job, and subagent RPCs')
  it.todo('keeps secrets and prompt bodies out of fixtures and diagnostic snapshots')
  it.todo('fails explicitly when the connected DSH version is unsupported')
})
